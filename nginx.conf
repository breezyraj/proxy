server {
      listen 80;

      # API requests → backend
      location /api/ {
          proxy_pass http://localhost:8080/;
      }

      # Everything else → UI
      location / {
          proxy_pass http://localhost:3000/;
      }
  }